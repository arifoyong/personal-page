import ChatWidget from "@/components/chatWidget"

export default function Home() {
  return (
    <main className="flex min-h-screen bg-blue-100">
      {/* resume */}
      <div className="flex w-full p-2 gap-2">
        <section className="flex flex-1 bg-gray-100">
          l
        </section>
        <section className="flex flex-1 bg-gray-100">
          r
        </section>
      </div>

    </main>
  )
}
